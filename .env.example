WP_PORT=8080

DB_HOST=mariadb
DB_NAME=wp_local
DB_USER=wpuser
DB_PASSWORD=wppass
DB_ROOT_PASSWORD=toor

# SMTP Email Configuration
# Development (MailHog): Leave empty or use defaults
# SMTP_HOST=mailhog (default)
# SMTP_PORT=1025 (default)
# No SMTP_USER or SMTP_PASS needed for MailHog

# Production (Gmail): Uncomment and configure
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_SECURE=tls
# SMTP_FROM=your-email@gmail.com
# SMTP_FROM_NAME=Your Website Name

# Development settings
# Set to 0 to disable contact form rate limiting during development
CONTACT_FORM_RATE_LIMIT=0